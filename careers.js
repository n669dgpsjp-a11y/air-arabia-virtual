const API='https://hhfzzchoqibggtqvhkub.supabase.co/functions/v1/airline-api',$=id=>document.getElementById(id);let jobs=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function filters(){[...new Set(jobs.map(j=>j.role))].sort().forEach(v=>$('roleFilter').insertAdjacentHTML('beforeend',`<option>${esc(v)}</option>`));[...new Set(jobs.map(j=>j.base))].sort().forEach(v=>$('baseFilter').insertAdjacentHTML('beforeend',`<option>${esc(v)}</option>`))}function render(){const r=$('roleFilter').value,b=$('baseFilter').value,q=$('jobSearch').value.toLowerCase(),list=jobs.filter(j=>(!r||j.role===r)&&(!b||j.base===b)&&(!q||`${j.title} ${j.description}`.toLowerCase().includes(q)));$('jobsGrid').innerHTML=list.length?list.map(j=>`<a class="job-card-link" href="job-detail.html?id=${encodeURIComponent(j.id)}" aria-label="View ${esc(j.title)} vacancy"><article class="job-card"><span class="section-kicker">${esc(j.role).toUpperCase()}</span><h3>${esc(j.title)}</h3><div class="job-meta"><span class="job-chip">${esc(j.role)}</span><span class="job-chip">${esc(j.base)}</span></div><p>${esc(j.description)}</p><span class="job-open">View job description <span aria-hidden="true">→</span></span></article></a>`).join(''):'<div class="empty-state" role="status">No vacancies match these filters.</div>'}['roleFilter','baseFilter'].forEach(id=>$(id).onchange=render);$('jobSearch').oninput=render;async function load(){try{const r=await fetch(API+'?action=public'),d=await r.json();jobs=d.jobs||[];filters();render()}catch{$('jobsGrid').innerHTML='<div class="empty-state" role="status">Vacancies are temporarily unavailable.</div>'}}load();const menuBtn=$('menuBtn'),mainNav=$('mainNav');menuBtn?.addEventListener('click',()=>{const open=mainNav?.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(Boolean(open)));menuBtn.setAttribute('aria-label',open?'Close navigation menu':'Open navigation menu')});